# ===========================================
# Vercel / Production va lokal uchun kalitlar
# ===========================================
# Nusxa: bu faylni .env nomiga ko'chiring va qiymatlarni to'ldiring.
#   cp .env.example .env
# Vercel da: Project → Settings → Environment Variables da shu kalitlarni qo'shing.

# --- Majburiy (Vercel va Neon bilan lokal) ---

# Neon PostgreSQL ulanish qatori (Neon dashboard dan oling)
DATABASE_URL=postgresql://USER:PASSWORD@HOST/DATABASE?sslmode=require

# Django secret key (production da yangi random kalit yarating)
# https://djecrety.ir/ yoki: python -c "from django.core.management.utils import get_random_secret_key; print(get_random_secret_key())"
SECRET_KEY=your-secret-key-here

# --- Production (Vercel) uchun ---

# Vercel da: 0 yoki false. Lokalda: 1 yoki true
DEBUG=0

# Vercel da: domenlar vergul bilan (*.vercel.app va custom domain)
# Masalan: .vercel.app,yourapp.vercel.app
ALLOWED_HOSTS=.vercel.app,yourapp.vercel.app

# ===========================================
# Ixtiyoriy: faqat lokal PostgreSQL ishlatilsa (DATABASE_URL bo'lmasa)
# ===========================================
# PGDATABASE=leetcode_db
# PGUSER=postgres
# PGPASSWORD=1
# PGHOST=localhost
# PGPORT=5433
